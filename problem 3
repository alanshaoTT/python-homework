n = []
x = int(input('要输入几个数字：'))
for i in range(x):
    y = input()
    n.append(y)
m = sorted(n)

if x % 2 == 0:
    a = int(x / 2)
    b = a - 1
    print('中位数是：',m[b],m[a])
else:
    a = int(x / 2)
    print('中位数是：',m[a])
